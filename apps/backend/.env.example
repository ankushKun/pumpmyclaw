# Telegram bot token (platform bot — used for OAuth HMAC verification)
TELEGRAM_BOT_TOKEN=your-platform-bot-token

# JWT secret for signing authentication tokens
# IMPORTANT: Generate a strong random secret for production!
# Example: openssl rand -hex 32
JWT_SECRET=your-jwt-secret-change-in-production

# JWT token expiry (e.g., "7d", "24h", "1h")
JWT_EXPIRY=7d

# Encryption key for secrets at rest (AES-256-GCM)
# IMPORTANT: Generate a 32-byte hex key for production!
# Example: openssl rand -hex 32
ENCRYPTION_KEY=your-encryption-key-change-in-production

# Frontend URL (for CORS in production)
FRONTEND_URL=http://localhost:3000

# Backend public URL (used for NOWPayments webhook callback URL)
# In production, set to your public backend domain (e.g., https://backend.pumpmyclaw.fun)
# For local testing with ngrok: https://abc123.ngrok.io
BACKEND_URL=http://localhost:8080

# SQLite database path
DATABASE_URL=./data/pmc.db
DB_PASS=PASS

# Server port
PORT=8080

# Environment (development | production)
NODE_ENV=development

# Docker socket path (default: /var/run/docker.sock)
# Colima: ~/.colima/default/docker.sock
# Docker Desktop (macOS): ~/.docker/run/docker.sock
DOCKER_SOCKET=/var/run/docker.sock

# Base directory for per-instance OpenClaw data (bind-mounted into containers)
INSTANCES_DATA_DIR=./data/instances

# ── NOWPayments (crypto payments) ───────────────────────────────
# API key from NOWPayments dashboard
NOWPAYMENTS_API_KEY=your-nowpayments-api-key

# IPN (webhook) secret key for verifying payment notifications
NOWPAYMENTS_IPN_SECRET=your-ipn-secret-key

# Set to "true" to use sandbox (api-sandbox.nowpayments.io)
NOWPAYMENTS_SANDBOX=true